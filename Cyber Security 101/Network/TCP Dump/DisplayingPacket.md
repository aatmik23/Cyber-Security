
- `q`: Quick output; print brief packet information
- `-e`: Print the link-level header
- `-A`: Show packet data in ASCII
- `-xx`: Show packet data in hexadecimal format, referred to as hex
- `-X`: Show packet headers and data in hex and ASCII


include the MAC addresses in Tcpdump output, all you need to do is to add `-e`


prefer shorter output lines, you can opt for “quick” output with `-q`.
timestamp, along with the source and destination IP addresses and source and destination port numbers.

ASCII codes represent text. In other words, you can expect `-A` to display all the bytes mapped to English letters, numbers, and symbols.


![](../../assets/Pasted%20image%2020260805004018.png)

we dont use and with it

