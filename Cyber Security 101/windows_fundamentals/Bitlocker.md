**BitLocker Drive Encryption** is a **full-disk encryption** feature built into certain editions of Microsoft Windows. It protects the data stored on a drive by encrypting the entire drive, making the data unreadable without the correct authentication.

